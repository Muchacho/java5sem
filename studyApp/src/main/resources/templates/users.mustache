<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    <form action="/logout" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
        <input type="submit" value="Sign Out"/>
    </form>
    <form action="http://localhost:8081/findUsers" method="post">
        <input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
        <input type="text" name="login" placeholder="student login"/>
        <button type="submit">find</button>
    </form>
    <h1>Students list</h1>
    <h1>users</h1>
    {{#users}}
        <div>
            <b>{{id}}</b>
            <b>{{name}}</b>
            <b>{{course}}</b>
            <b>{{role}}</b>
        </div>
    {{/users}}
</body>
</html>